[project]
name = "bede"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["bede-core/tests", "bede-data/tests", "bede-data-mcp/tests"]
